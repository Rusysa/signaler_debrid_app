services:
  - type: web
    name: watchparty-signaler
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 15325
      - key: WERON_BACKEND
        value: memory://
